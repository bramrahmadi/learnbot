apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: learnbot
  labels:
    app: api-gateway
    app.kubernetes.io/name: api-gateway
spec:
  type: ClusterIP
  selector:
    app: api-gateway
  ports:
    - name: http
      port: 8090
      targetPort: http
      protocol: TCP
